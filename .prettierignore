.next
node_modules
.pnpm-store
coverage
playwright-report
test-results
public/sw.js
docs/design/concepts-10
docs/design/ticket-concepts-10
docs/design/ticket-directions
docs/design/assets
*.png
pnpm-lock.yaml

# Preserve pre-existing product evidence and historical/user-authored records byte-for-byte.
# Newly generated release Markdown is checked separately in the documentation audit.
**/*.md
**/*.log
**/*.txt

# 디자인 보드는 눈으로 판정하는 증거물이다. Prettier의 HTML 재정렬은 inline 요소 사이의
# 공백을 바꿔 렌더 결과를 건드릴 수 있으므로 바이트 그대로 둔다(같은 폴더의 .mjs/.js 도구는 검사 대상).
design-lab/**/*.html
design-lab/**/*.css
docs/design/**/*.html
docs/design/**/*.css
